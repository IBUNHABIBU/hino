<!DOCTYPE html>
<html>
  <head>
    <title>Ecohygiene</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Ecohygiene deals with drink factory hygiene solution">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
<!-- Before (causing the error) -->
<%= javascript_include_tag "application", type: "module", "data-turbo-track": "reload" %>


<%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

<link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.7.2/css/all.css"> <link rel="preconnect" href="https://fonts.googleapis.com">
 <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
 <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700;800&display=swap" rel="stylesheet">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'layouts/header/nav' %>
    <%= render 'layouts/flashes' %>
      <%= yield %>
    <%= render 'layouts/footer' %>
  </body>
</html>
